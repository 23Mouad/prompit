export const metadata = {
   title: "Profile",
   description: "Profile",
   openGraph: {
      title: "Profile",
      description: "Profile",
   },
};
export default function CreateLayout({ children }) {
   return <section>{children} </section>;
}
