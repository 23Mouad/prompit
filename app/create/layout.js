export const metadata = {
   title: "Create prompt",
   description:
      "Create prompt with title and description and tags with your account",
   openGraph: {
      title: "Create prompt",
      description:
         "Create prompt with title and description and tags with your account",
   },
};
export default function CreateLayout({ children }) {
   return <section>{children} </section>;
}
