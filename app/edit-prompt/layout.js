export const metadata = {
   title: "Edit prompt",
   description:
      "Edit prompt's title and description and tags with your account",
   openGraph: {
      title: "Edit prompt",
      description:
         "Edit prompt's title and description and tags with your account",
   },
};
export default function CreateLayout({ children }) {
   return <section>{children} </section>;
}
